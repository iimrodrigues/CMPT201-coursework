#include <pthread.h>
#include <stdio.h>

void *func() {
  sleep(3);
  return 0;
}

int main() {
  pthread_t thread;
  int status = pthread_create(&thread, NULL, func, NULL);
  if (status != 0) {
    printf("womp womp\n");
  } else {
    printf("i was created\n");
  }
}
