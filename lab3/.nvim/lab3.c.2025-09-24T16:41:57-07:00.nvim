#define _POSIX_C_SOURCE 200809
#include <stdio.h>
#include <stdlib.h>
#include <string.h>

int main() {
  char *input = NULL;
  size_t len = 0;

  printf("Enter input: ");
  getline(&input, &len, stdin);

  printf("%s", input);
}
