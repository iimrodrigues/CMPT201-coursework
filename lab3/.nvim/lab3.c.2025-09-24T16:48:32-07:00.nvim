#define _POSIX_C_SOURCE 200809
#include <stdbool.h>
#include <stdio.h>
#include <stdlib.h>
#include <string.h>

int main() {
  char *input = NULL;
  size_t len = 0;

  while (true) {
    printf("Enter input: ");
    getline(&input, &len, stdin);
    printf("%s", input);
  }

  free(input);
}
